# A tuple is an immutable data type similar to a list.
# A tuple is also a sequence, which means it can be indexed and sliced.
# A tuple is also an iterable, which means it can be used in a for loop.
# A tuple is also a container, which means it can hold other tuples.

parents_1 = ("Salim", "Zainab")
print(parents_1)

parents_2 = "Khalid", "Hassana"
print(parents_2)

print(len(parents_1))
print(parents_1[0])
